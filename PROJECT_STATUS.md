# 프로젝트 현황 보고서

## 현재 상태
- 도서 등록, 검색, 상태 필터, 정렬, 상세 조회가 동작합니다.
- 독서 로그는 페이지 범위 검증과 함께 저장되며 진행률을 자동으로 갱신합니다.
- 리뷰와 별점은 상세 화면에서 저장/수정할 수 있습니다.
- 태그/키워드를 책에 추가·삭제할 수 있으며, 태그로 검색이 가능합니다.
- 대시보드는 읽는 중인 책, 완독한 책, 누적 페이지를 보여줍니다.
- 대시보드에 GitHub 잔디 스타일의 52주 독서 활동 히트맵이 표시됩니다.
- 대시보드에 최근 12개월 읽은 페이지/완독 권수 월별 바 차트가 표시됩니다.
- 테마 토글은 라이트/다크 모드를 지원하며 `localStorage`에 저장됩니다.
- 스토어는 입력 검증, 상태 전이, 구버전 데이터 정규화를 담당합니다.
- GitHub Actions CI에서 단위 테스트와 Playwright E2E 테스트가 병렬로 실행됩니다.

## 이번에 보강된 항목 (2026-04-10)
- 계층적 AGENTS.md 문서 생성 (루트 포함 11개 파일)
- picomatch 중첩 취약점 수정 (resolutions로 4.0.3 강제 고정)
- CI에 Playwright E2E job 추가 (단위 테스트와 병렬 실행, 실패 시 아티팩트 보관)
- 태그/키워드 기능: Book 타입에 `tags` 필드, `updateTags` store 액션, 상세 화면 UI, 목록 카드 배지 및 검색 지원
- 독서 활동 히트맵: 날짜별 읽은 페이지 집계, 4단계 색상, 월·요일 레이블, 범례
- 월별/연간 통계 바 차트: Pages/Books 탭 전환, 연간 요약 수치, 바 호버 툴팁
- E2E 시나리오 확장: 3개 → 11개 (삭제, 태그, 유효성 검사, 지속성, 대시보드, 상태 변경)

## 남은 과제
1. **Supabase 백엔드 연동** — localStorage MVP → Supabase DB, 인증(Auth), 멀티 디바이스 동기화
2. 외부 도서 검색 API 연동 (알라딘/교보문고)
3. 소셜 기능 (친구와 책 공유, 코멘트)
4. 독서 타이머 기능
